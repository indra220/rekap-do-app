name: Build and Release Electron App

# PENGATURAN: Jalan automatik setiap kali ada push tag 'v*' ke repository
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  release:
    runs-on: windows-latest 
    permissions:
      contents: write 

    steps:
      - name: 1. Muat turun kod dari Repository
        uses: actions/checkout@v4

      - name: 2. Mempersiapkan Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'npm'

      - name: 3. Memasang Dependencies
        run: npm install

      - name: 4. Build Aplikasi dan Publish
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          FULL_COMMIT_MSG: ${{ github.event.head_commit.message }}
        run: |
          # Mengambil KESELURUHAN baris deskripsi commit tanpa terpotong
          SAFE_MSG=$(echo "$FULL_COMMIT_MSG" | python3 -c "import sys, json; text = sys.stdin.read().strip(); print(json.dumps(text).strip('\"'))")

          cat <<EOF > update-config.json
          {
            "releaseInfo": {
              "releaseNotes": "$SAFE_MSG"
            }
          }
          EOF

          echo "Fail update-config.json berjaya dibuat:"
          cat update-config.json

          # Melaksanakan arahan build yang biasa anda gunakan
          npm run dist -- --publish always

      - name: 5. Mengunggah fail tambahan ke GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist/*.exe
            dist/*.exe.blockmap
            dist/latest.yml
            update-config.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}